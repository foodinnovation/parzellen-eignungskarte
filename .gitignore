.DS_Store
__pycache__/
*.pyc

# Bundled on purpose (national context, small):
#   bfs_cantons.json — BFS canton context for the choropleth
#   cantons.geojson  — canton boundaries for the choropleth
#   climate.json     — coarse GDD grid for the climate factor

# Local ETL output — regenerate per canton with build_canton.py --geoadmin.
# Auto-loaded if placed next to the HTML; not shipped (the hosted map is live).
parcels_scored.geojson
